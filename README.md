# 我的家 - 家庭物品收纳管理系统

一个现代化的家庭物品收纳管理应用，帮助您轻松管理家庭物品的存放记录与查找。

## 功能特性

- 🏠 **空间管理** - 自定义管理卧室、客厅等家庭空间
- 📋 **物品管理** - 记录物品信息、分类、标签和图片
- 🔍 **智能搜索** - 快速查找存放的物品
- 👥 **多人协作** - 家庭成员共同管理
- 📱 **响应式设计** - 完美适配移动端和桌面端
- 🌙 **暗色模式** - 支持明暗主题切换
- 📊 **数据统计** - 直观展示收纳情况

## 技术栈

- **框架**: React 19.2.0
- **构建工具**: Vite 7.2.7
- **语言**: TypeScript 5.9.2
- **UI 组件**: Ant Design 6.1.0
- **样式**: UnoCSS 0.58.x
- **状态管理**: Zustand 5.0.9
- **路由**: React Router 7.10.1
- **数据获取**: TanStack Query 5.84.1

## 快速开始

### 环境要求

- Node.js >= 20.0.0
- npm >= 8.0.0 或 pnpm >= 8.0.0

### 安装依赖

```bash
# 使用 npm
npm install

# 使用 pnpm（推荐）
pnpm install
```

### 配置环境变量

复制环境变量模板并修改配置：

```bash
cp .env.example .env
```

### 开发

```bash
# 启动开发服务器
npm run dev
# 或
pnpm dev
```

### 构建生产版本

```bash
npm run build
# 或
pnpm build
```

### 预览生产版本

```bash
npm run preview
# 或
pnpm preview
```

## 项目结构

```
src/
├── components/      # 组件
│   ├── ui/         # 基础 UI 组件
│   ├── layout/     # 布局组件
│   ├── forms/      # 表单组件
│   └── charts/     # 图表组件
├── pages/          # 页面组件
│   ├── auth/       # 认证相关页面
│   ├── dashboard/  # 仪表板
│   ├── spaces/     # 空间管理
│   ├── items/      # 物品管理
│   └── settings/   # 设置页面
├── stores/         # Zustand 状态管理
│   ├── auth/       # 认证状态
│   ├── spaces/     # 空间状态
│   └── items/      # 物品状态
├── services/       # API 服务
├── types/          # TypeScript 类型定义
├── hooks/          # 自定义 Hooks
├── utils/          # 工具函数
├── assets/         # 静态资源
└── styles/         # 样式文件
```

## 开发规范

### 代码规范

- 使用 ESLint + Prettier 进行代码格式化
- 组件命名使用 PascalCase
- 文件命名使用 kebab-case
- 使用 TypeScript 严格模式

### Git 提交规范

遵循 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/) 规范：

```
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

类型说明：

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行的变动）
- `refactor`: 重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动

### 分支策略

- `main`: 主分支，用于生产环境
- `develop`: 开发分支，用于集成功能
- `feat/*`: 功能分支
- `fix/*`: 热修复分支

## 脚本命令

```bash
# 开发
npm run dev          # 启动开发服务器
npm run preview      # 预览生产版本

# 构建
npm run build        # 构建生产版本
npm run type-check   # TypeScript 类型检查

# 代码质量
npm run lint         # ESLint 检查
npm run lint:fix     # 自动修复 ESLint 问题
npm run format       # Prettier 格式化

# 测试
npm run test         # 运行测试
npm run test:ui      # 测试 UI 界面
npm run test:coverage # 测试覆盖率
```

## 许可证

[MIT](LICENSE)

## 贡献

欢迎提交 Issue 和 Pull Request！
